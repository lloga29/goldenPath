# Validation blueprint for a standalone GitOps repository.
name: Validate Manifests

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'v1.28.0'

      - name: Setup kustomize
        uses: imranismail/setup-kustomize@v2
        with:
          kustomize-version: '5.3.0'

      - name: Validate Kustomize builds
        shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r dir; do
            echo "Validating Kustomize build: $dir"
            kustomize build "$dir" >/dev/null
          done < <(find gitops-config -name kustomization.yaml -exec dirname {} \; | sort -u)

      - name: Validate YAML syntax
        shell: bash
        run: |
          set -euo pipefail
          python3 -m pip install --disable-pip-version-check --quiet pyyaml
          while IFS= read -r file; do
            echo "Checking YAML: $file"
            python3 - "$file" <<'PY'
          import sys
          import yaml
          with open(sys.argv[1], encoding="utf-8") as handle:
              list(yaml.safe_load_all(handle))
          PY
          done < <(find gitops-config -type f \( -name '*.yaml' -o -name '*.yml' \) | sort)

      - name: Validate rendered Kubernetes manifests
        shell: bash
        run: |
          set -euo pipefail
          curl -fsSLo kubeval.tar.gz https://github.com/instrumenta/kubeval/releases/download/v0.16.1/kubeval-linux-amd64.tar.gz
          tar -xzf kubeval.tar.gz
          sudo install -m 0755 kubeval /usr/local/bin/kubeval

          while IFS= read -r dir; do
            echo "Schema-validating rendered manifests: $dir"
            kustomize build "$dir" | kubeval --strict --ignore-missing-schemas
          done < <(find gitops-config/apps gitops-config/platform -name kustomization.yaml -exec dirname {} \; | sort -u)

      - name: Setup Conftest
        shell: bash
        run: |
          set -euo pipefail
          curl -fsSLo conftest.tar.gz https://github.com/open-policy-agent/conftest/releases/download/v0.45.0/conftest_0.45.0_Linux_x86_64.tar.gz
          tar -xzf conftest.tar.gz
          sudo install -m 0755 conftest /usr/local/bin/conftest

      - name: Run application policy checks
        shell: bash
        run: |
          set -euo pipefail
          if [[ -d platform-policies/kubernetes ]]; then
            POLICY_DIR="platform-policies/kubernetes"
          elif [[ -d ../platform-policies/kubernetes ]]; then
            POLICY_DIR="../platform-policies/kubernetes"
          else
            echo "ERROR: Kubernetes Conftest policy bundle was not found."
            echo "Check out the platform-policies repository next to this repository or run from the consolidated Golden Path repository."
            exit 1
          fi

          mapfile -t deployments < <(find gitops-config/apps -name deployment.yaml | sort)
          if (( ${#deployments[@]} == 0 )); then
            echo "ERROR: no application Deployment manifests were found for policy validation."
            exit 1
          fi

          conftest test "${deployments[@]}" --policy "$POLICY_DIR"
