# EXAMPLE ONLY. This file is not reconciled by the Golden Path ApplicationSets.
# Replace every placeholder and complete the authentication/security design
# before promoting an equivalent resource into environment-owned desired state.
apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: vault-backend-example
spec:
  provider:
    vault:
      server: https://vault.example.invalid
      path: secret
      version: v2
      auth:
        kubernetes:
          mountPath: kubernetes
          role: external-secrets
          serviceAccountRef:
            name: external-secrets
            namespace: external-secrets
