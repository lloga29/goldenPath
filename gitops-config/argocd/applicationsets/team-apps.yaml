# ApplicationSet for team-owned applications.
# Development and staging are automatically reconciled. Production is manual.
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: team-applications
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]

  generators:
    - matrix:
        generators:
          - list:
              elements:
                - team: payments
                  repo: git@github.com:org/team-payments-gitops.git
                - team: orders
                  repo: git@github.com:org/team-orders-gitops.git

          - list:
              elements:
                - env: dev
                  cluster: https://dev-cluster.example.com
                  autoSync: true
                  prune: true
                - env: staging
                  cluster: https://staging-cluster.example.com
                  autoSync: true
                  prune: false
                - env: prod
                  cluster: https://prod-cluster.example.com
                  autoSync: false
                  prune: false

  template:
    metadata:
      name: '{{.team}}-{{.env}}'
      labels:
        team: '{{.team}}'
        environment: '{{.env}}'

    spec:
      project: '{{.team}}'

      source:
        repoURL: '{{.repo}}'
        targetRevision: HEAD
        path: 'apps/overlays/{{.env}}'

      destination:
        server: '{{.cluster}}'
        namespace: '{{.team}}-{{.env}}'

      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - PrunePropagationPolicy=foreground

        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m

  # `automated` is deliberately omitted for production. Keeping this in a
  # template patch prevents a false "autoSync: false" flag from leaving the
  # automated sync block enabled.
  templatePatch: |
    {{- if .autoSync }}
    spec:
      syncPolicy:
        automated:
          prune: {{ .prune }}
          selfHeal: true
    {{- end }}
