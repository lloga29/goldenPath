# Reject mutable or implicit image references at admission time.
apiVersion: constraints.gatekeeper.sh/v1beta1
kind: K8sImmutableImages
metadata:
  name: require-immutable-images
  annotations:
    argocd.argoproj.io/sync-wave: "20"
spec:
  match:
    excludedNamespaces:
      - kube-system
      - gatekeeper-system
    kinds:
      - apiGroups: [""]
        kinds: ["Pod"]
      - apiGroups: ["apps"]
        kinds: ["Deployment", "StatefulSet", "DaemonSet"]
