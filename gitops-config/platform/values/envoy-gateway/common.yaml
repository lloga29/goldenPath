deployment:
  replicas: 2
  envoyGateway:
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 512Mi
config:
  envoyGateway:
    logging:
      level:
        default: info
