name: Rebuild logo asset

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  rebuild-logo:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Temporarily paused while the staged PNG payload is refreshed.'
