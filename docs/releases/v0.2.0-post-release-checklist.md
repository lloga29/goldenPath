# GoldenPath v0.2.0 Post-release Verification Checklist

**Production validation: NOT CLAIMED.**

After publishing v0.2.0, verify:

- [ ] tag `v0.2.0` resolves to the intended verified release commit;
- [ ] GitHub release references the same commit/tag;
- [ ] repository validation on the release commit is green;
- [ ] Runtime Lab on the release commit is green;
- [ ] the retained P5 evidence bundle identifies the same source revision;
- [ ] receipt decision is `VERIFIED`;
- [ ] independent verification is `PASS`;
- [ ] continuous assurance current state is `VERIFIED_HEALTHY` or `RECOVERED_REVERIFIED`;
- [ ] scorecard repository/runtime evidence is `VERIFIED`;
- [ ] scorecard production evidence is `NOT_CLAIMED`;
- [ ] release notes and public docs still preserve the evidence boundary;
- [ ] no release tag was moved after publication.

If any identity differs, treat the release as unverified until fresh evidence is produced for the exact published commit.
